const queryKeys = {
    monthlyRecurring: ["monthly-recurring"],
    monthlyTrades: ["monthly-trades"],
    tickerTrades: ["ticker-trades"],
    trades: ["trades"],
    weeklyTrades: ["weekly-trades"],
    yearlyRecurring: ["yearly-recurring"],
}

export default queryKeys
